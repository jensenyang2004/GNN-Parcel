# PARCEL — Partitioned Attention-based Reverse Curricula for Enhanced Learning
# Built on top of: https://github.com/thomyphan/rl4mapf
#
# Install with:
#   pip install -r requirements.txt
#
# For GPU support, replace the torch line with the appropriate CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pygame>=2.1.0
mlflow>=2.10.0